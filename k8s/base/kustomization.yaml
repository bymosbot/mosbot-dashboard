apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mosbot

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - configmap.yaml

commonLabels:
  app: mosbot-dashboard
  component: frontend
